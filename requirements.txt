# --- Alap Rendszer (FastAPI mag) ---
fastapi
uvicorn
python-dotenv
python-multipart      # Fájl feltöltéshez (Könyvek, PDF-ek)
aiofiles              # Aszinkron fájlkezelés

# --- Az Agy (AI & Logika) ---
openai
jinja2
tiktoken              # Token számlálás (Hogy pontosabb legyen a memória)

# --- Hacker & Scraper Eszközök (Adatbányászat) ---
requests
beautifulsoup4
httpx                 # Sokkal gyorsabb, aszinkron kérések (szkenneléshez)
fake-useragent        # Hogy ne ismerjék fel, hogy bot vagy (rejtőzködés)
pypdf                 # PDF OLVASÓ (Hogy be tudja "falni" a szakkönyveket!)

# --- Banki & Pénzügyi Modul (A Pénzcsináló) ---
pandas                # Adataelemzés, Excel kezelés, Tőzsdei adatok
numpy                 # Brutális matek (MLM mátrixokhoz, kamatszámításhoz)
openpyxl              # Excel generálás (Jelentésekhez)
ccxt                  # KRIPTO TŐZSDE MOTOR (A világ összes tőzsdéjét kezeli)

# --- Adatbázisok (Memória & Széf) ---
motor                 # MongoDB (AI Memóriának tökéletes)
sqlalchemy            # SQL (Banki tranzakciókhoz ez KELL)
psycopg2-binary       # PostgreSQL driver (Standard)
asyncpg               # ASZINKRON SQL (Ez teszi banki sebességűvé az adatbázist)

# --- Biztonság (Hacker védelem) ---
python-jose[cryptography]
passlib[bcrypt]
cryptography          # AES titkosítás a tárolt adatokhoz

# --- Kézbesítés ---
PyGithub